
@*@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<input id="ID" value="Show" type="button"/>
<br />
<canvas id="myChart" style="max-width:400px;max-height:400px"></canvas>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
	$(function(){
        $("#ID").click(function() {
            $.ajax({
			type: "POST",
			url: "/Home/GetBillChart",
			data: "",
            contextType: "application/json; charset=utf-8",
			dataType: "json",
			success: OnSuccessResult,
			error: OnError
        });

        function OnSuccessResult(data) {
            var _data = data;
            var _charLabels = _data[0];
            var _charData = _data[1];
            var barColor = ["red", "green", "blue", "orange", "brown", "purple", "yellow"];
            new Chart("myChart",
                {
                    type: "bar",
                    data: {
                        labels: _chartLabels,
                        datasets: [{
                            backgroundColor: barColor,
                            data: _chartData
                        }]
                    }
                });
		}
        function OnError(err) { }
        })
	})
</script>*@
